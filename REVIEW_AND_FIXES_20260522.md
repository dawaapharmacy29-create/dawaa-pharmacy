# تعديلات الاستقرار والمزامنة - 2026-05-22

## ما تم تعديله

1. **مزامنة العملاء بعد استيراد فواتير المبيعات**
   - تعديل `src/lib/invoiceImporter.ts`
   - بعد كل استيراد مبيعات يتم إعادة بناء بيانات العملاء المتأثرين من جدول `sales_invoices`.
   - يتم تحديث:
     - آخر شراء
     - أول شراء
     - إجمالي الفواتير
     - إجمالي المشتريات
     - متوسط الفاتورة
     - المتوسط الشهري
     - حالة الاحتفاظ
     - تصنيف العميل

2. **SQL لإصلاح البيانات القديمة**
   - إضافة:
     - `supabase/20260522_sync_customer_analysis_from_invoices.sql`
   - شغّل هذا الملف مرة واحدة في Supabase لإعادة بناء `customer_analysis` من كل الفواتير الموجودة.

3. **إصلاح حساب الحوافز**
   - تعديل `src/components/points/SalaryCalculator.tsx`
   - الحافز النهائي أصبح = النقاط النهائية × قيمة النقطة.
   - تم منع حساب المكافآت والخصومات مرتين في PDF.

4. **قاعدة تكرار نفس الخطأ**
   - تعديل `src/lib/pointsWorkflow.ts`
   - تعديل `src/pages/Reviews.tsx`
   - التكرار أصبح خطيًا داخل دورة 26→25:
     - أول مرة = قيمة البند
     - ثاني مرة = ×2
     - ثالث مرة = ×3
     - رابع مرة = ×4

5. **بحث العميل في تسجيل صرف الصنف الراكد**
   - تعديل `src/pages/StagnantMedicines.tsx`
   - إضافة مربع بحث فوق اختيار العميل.
   - يدعم البحث بالاسم أو الكود أو الهاتف واستخدام النجمة `*` مثل:
     - `*ا*سلا*م`

6. **تحسين صفحة حسابات وصلاحيات الفريق**
   - تعديل `src/pages/StaffAccounts.tsx`
   - تحويل الجدول إلى كروت منظمة:
     - اسم الموظف
     - الدور
     - الفرع
     - اسم المستخدم
     - كلمة المرور
     - الحالة
     - الصلاحيات تحت بيانات الموظف في مساحة واسعة
     - أزرار التعديل والتعطيل والفتح

## خطوات ضرورية بعد رفع النسخة

1. انسخ ملف `.env` القديم داخل المشروع.
2. شغّل SQL:
   - `supabase/20260522_sync_customer_analysis_from_invoices.sql`
3. شغّل:
   - `npm.cmd install`
   - `npm.cmd run build`
4. جرّب:
   - استيراد فواتير جديدة
   - فتح صفحة العملاء والتأكد أن آخر شراء اتحدث
   - تصدير PDF حوافز أحمد البطل
   - تكرار خصم نفس البند لنفس الموظف داخل نفس الدورة
