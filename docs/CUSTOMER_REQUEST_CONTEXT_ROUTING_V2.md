# Customer request context routing V2

This branch fixes deep-link context for customer requests so customer, product, branch and registrar filters are preserved instead of falling back to the general requests page.
