const fs = require('fs');
const file = 'src/pages/CustomerRequests.tsx';
let src = fs.readFileSync(file, 'utf8');
function replaceOnce(oldText, newText, label) {
  if (!src.includes(oldText)) throw new Error(`${label} anchor not found`);
  src = src.replace(oldText, newText);
}

replaceOnce('  Clock3,\n  Database,', '  Clock3,\n  Copy,\n  Database,', 'Copy import');

const helperAnchor = `function nextAction(request: CustomerRequest) {`;
const helpers = `function requestContactMessage(request: CustomerRequest) {\n  return \`أهلاً \${request.customer_name || 'حضرتك'}، مع حضرتك صيدليات دواء بخصوص طلب صنف \${request.medicine_name}.\`;\n}\n\nfunction openRequestWhatsApp(request: CustomerRequest) {\n  if (!request.customer_phone) return toast.error('لا يوجد رقم هاتف صالح للعميل');\n  window.open(generateWhatsAppLink(request.customer_phone, requestContactMessage(request)), '_blank', 'noopener,noreferrer');\n}\n\nfunction callRequestCustomer(request: CustomerRequest) {\n  if (!request.customer_phone) return toast.error('لا يوجد رقم هاتف صالح للعميل');\n  window.location.href = \`tel:\${request.customer_phone}\`;\n}\n\nasync function copyRequestCustomerPhone(request: CustomerRequest) {\n  const phone = displayEgyptianPhone(request.customer_phone || '') || request.customer_phone || '';\n  if (!phone) return toast.error('لا يوجد رقم هاتف صالح للعميل');\n  try {\n    await navigator.clipboard.writeText(phone);\n    toast.success('تم نسخ رقم العميل');\n  } catch {\n    toast.error('تعذر نسخ الرقم تلقائيًا');\n  }\n}\n\n`;
replaceOnce(helperAnchor, helpers + helperAnchor, 'contact helpers');

const cardFields = `<div className="mt-4 grid grid-cols-2 gap-3 md:grid-cols-4"><CardField label="العميل" value={request.customer_name || 'غير محدد'} /><CardField label="كود العميل" value={request.customer_code || '—'} /><CardField label="الهاتف" value={displayEgyptianPhone(request.customer_phone || '') || 'بدون هاتف'} /><CardField label="أهمية العميل" value={customerImportanceLabel(request)} /><CardField label="المسجل" value={registrarName(request)} /><CardField label="المسئول الحالي" value={currentOwner(request)} /><CardField label="وقت التسجيل" value={exactRequestTime(request)} /><CardField label="عمر الطلب" value={ageLabel(request)} /></div>\n    <div className={\`mt-4 rounded-2xl border px-4 py-3 text-sm font-black \${overdue ? 'border-red-400/25 bg-red-500/10 text-red-100' : 'border-cyan-400/20 bg-cyan-500/[0.07] text-cyan-100'}\`}>الخطوة التالية: {nextAction(request)}</div>`;
const cardContact = `<div className="mt-4 grid grid-cols-2 gap-3 md:grid-cols-4"><CardField label="العميل" value={request.customer_name || 'غير محدد'} /><CardField label="كود العميل" value={request.customer_code || '—'} /><CardField label="الهاتف" value={displayEgyptianPhone(request.customer_phone || '') || 'بدون هاتف'} /><CardField label="أهمية العميل" value={customerImportanceLabel(request)} /><CardField label="المسجل" value={registrarName(request)} /><CardField label="المسئول الحالي" value={currentOwner(request)} /><CardField label="وقت التسجيل" value={exactRequestTime(request)} /><CardField label="عمر الطلب" value={ageLabel(request)} /></div>\n    <div className="mt-3 flex flex-wrap items-center gap-2 rounded-2xl border border-emerald-400/15 bg-emerald-500/[0.05] p-2.5">\n      <span className="ml-auto text-[10px] font-black text-slate-500">تواصل مباشر</span>\n      {request.customer_phone ? <>\n        <button type="button" onClick={() => openRequestWhatsApp(request)} className="flex items-center gap-1.5 rounded-xl border border-emerald-400/25 bg-emerald-500/10 px-3 py-2 text-[11px] font-black text-emerald-100 transition hover:bg-emerald-500/20" title="فتح واتساب برسالة جاهزة"><MessageCircle size={15} /> واتساب</button>\n        <button type="button" onClick={() => callRequestCustomer(request)} className="flex items-center gap-1.5 rounded-xl border border-sky-400/20 bg-sky-500/[0.08] px-3 py-2 text-[11px] font-black text-sky-100 transition hover:bg-sky-500/15" title="اتصال بالعميل"><Phone size={15} /> اتصال</button>\n        <button type="button" onClick={() => void copyRequestCustomerPhone(request)} className="flex items-center gap-1.5 rounded-xl border border-slate-600 bg-slate-900/70 px-3 py-2 text-[11px] font-black text-slate-300 transition hover:text-white" title="نسخ رقم العميل"><Copy size={14} /> نسخ الرقم</button>\n      </> : <span className="rounded-xl bg-red-500/10 px-3 py-2 text-[11px] font-black text-red-200">لا يوجد رقم صالح للتواصل</span>}\n    </div>\n    <div className={\`mt-4 rounded-2xl border px-4 py-3 text-sm font-black \${overdue ? 'border-red-400/25 bg-red-500/10 text-red-100' : 'border-cyan-400/20 bg-cyan-500/[0.07] text-cyan-100'}\`}>الخطوة التالية: {nextAction(request)}</div>`;
replaceOnce(cardFields, cardContact, 'card contact strip');

const tableAction = `<td className="px-3 py-3"><div className="flex items-center gap-1.5">{!customerRequestIsClosed(request) && !['cancelled', 'not_available'].includes(String(request.status || '')) && <button type="button" onClick={() => onAlternative(request)} className="flex items-center gap-1 rounded-xl border border-amber-400/20 bg-amber-500/[0.07] px-2.5 py-2 font-black text-amber-100 transition hover:bg-amber-500/15" title="إجراءات بديلة"><AlertTriangle size={14} /> أخرى</button>}<button type="button" onClick={() => onSelect(request)} className="flex items-center gap-1 rounded-xl bg-slate-800 px-3 py-2 font-black text-slate-200 transition hover:bg-cyan-500/15 hover:text-cyan-100"><Eye size={15} /> التفاصيل</button></div></td>`;
const tableContact = `<td className="px-3 py-3"><div className="flex items-center gap-1.5">{request.customer_phone && <><button type="button" onClick={() => openRequestWhatsApp(request)} className="flex h-9 w-9 items-center justify-center rounded-xl border border-emerald-400/20 bg-emerald-500/10 text-emerald-200 transition hover:bg-emerald-500/20" title="واتساب"><MessageCircle size={15} /></button><button type="button" onClick={() => callRequestCustomer(request)} className="flex h-9 w-9 items-center justify-center rounded-xl border border-sky-400/20 bg-sky-500/[0.08] text-sky-200 transition hover:bg-sky-500/15" title="اتصال"><Phone size={15} /></button></>}{!customerRequestIsClosed(request) && !['cancelled', 'not_available'].includes(String(request.status || '')) && <button type="button" onClick={() => onAlternative(request)} className="flex items-center gap-1 rounded-xl border border-amber-400/20 bg-amber-500/[0.07] px-2.5 py-2 font-black text-amber-100 transition hover:bg-amber-500/15" title="إجراءات بديلة"><AlertTriangle size={14} /> أخرى</button>}<button type="button" onClick={() => onSelect(request)} className="flex items-center gap-1 rounded-xl bg-slate-800 px-3 py-2 font-black text-slate-200 transition hover:bg-cyan-500/15 hover:text-cyan-100"><Eye size={15} /> التفاصيل</button></div></td>`;
replaceOnce(tableAction, tableContact, 'table contact actions');

fs.writeFileSync(file, src);
console.log('Applied customer request direct contact V15');
