const fs = require('fs');
const path = require('path');

const file = path.join(process.cwd(), 'src/pages/Reviews.tsx');
let src = fs.readFileSync(file, 'utf8');

function patch(label, from, to) {
  if (src.includes(to)) {
    console.log(`[smart-review-v9] ${label}: already applied`);
    return;
  }
  if (!src.includes(from)) throw new Error(`[smart-review-v9] ${label}: anchor not found`);
  src = src.replace(from, to);
  console.log(`[smart-review-v9] ${label}: applied`);
}

patch(
  'manual customer mode state',
  `  const [custSearched, setCustSearched] = useState(false);`,
  `  const [custSearched, setCustSearched] = useState(false);\n  const [smartManualCustomer, setSmartManualCustomer] = useState(false);`
);

patch(
  'reset manual customer mode',
  `    setCustSearched(false);\n    setRepeatInfo(null);`,
  `    setCustSearched(false);\n    setSmartManualCustomer(false);\n    setRepeatInfo(null);`
);

patch(
  'replace smart customer text field with lookup',
  `                <label className="space-y-1 text-xs font-black text-slate-300">\n                  <span className="flex items-center justify-between gap-2">\n                    <span>2) اسم العميل</span>\n                    <span className="text-[10px] font-bold text-slate-500">اختياري</span>\n                  </span>\n                  <input\n                    className="input-dark w-full"\n                    value={form.customerName}\n                    onChange={(e) => setForm((current) => ({ ...current, customerName: e.target.value }))}\n                    placeholder="اسم العميل"\n                    autoComplete="off"\n                  />\n                </label>`,
  `                <div className="space-y-1 text-xs font-black text-slate-300">\n                  <span className="flex items-center justify-between gap-2">\n                    <span>2) العميل</span>\n                    <button\n                      type="button"\n                      onClick={() => {\n                        setSmartManualCustomer((value) => !value);\n                        setCustHits([]);\n                        setCustSearched(false);\n                        if (!smartManualCustomer) setCustSearch('');\n                      }}\n                      className="text-[10px] font-black text-cyan-300 hover:text-cyan-200"\n                    >\n                      {smartManualCustomer ? 'بحث من العملاء' : 'عميل جديد / يدوي'}\n                    </button>\n                  </span>\n\n                  {!smartManualCustomer ? (\n                    <div className="relative">\n                      <input\n                        className="input-dark w-full pr-3 pl-16"\n                        value={custSearch}\n                        onChange={(e) => {\n                          setCustSearch(e.target.value);\n                          setCustSearched(false);\n                          if (e.target.value.trim().length < 2) setCustHits([]);\n                        }}\n                        onKeyDown={(e) => {\n                          if (e.key === 'Enter') {\n                            e.preventDefault();\n                            void loadCustomersHits();\n                          }\n                        }}\n                        placeholder="اسم / كود / موبايل"\n                        autoComplete="off"\n                      />\n                      <button\n                        type="button"\n                        onClick={() => void loadCustomersHits()}\n                        className="absolute left-1 top-1/2 -translate-y-1/2 rounded-lg border border-slate-600 px-2 py-1 text-[10px] font-black text-slate-200 hover:bg-slate-800"\n                      >\n                        بحث\n                      </button>\n                      {form.customerName ? (\n                        <div className="mt-1 flex items-center justify-between gap-2 rounded-lg border border-emerald-400/20 bg-emerald-500/5 px-2 py-1 text-[10px]">\n                          <span className="truncate text-emerald-100">{form.customerName}{form.customerCode ? ' · ' + form.customerCode : ''}</span>\n                          <button\n                            type="button"\n                            onClick={() => {\n                              setForm((f) => ({ ...f, customerId: '', customerCode: '', customerName: '', customerPhone: '', customerType: '' }));\n                              setCustSearch('');\n                            }}\n                            className="text-slate-400 hover:text-white"\n                          >\n                            تغيير\n                          </button>\n                        </div>\n                      ) : null}\n                      {custHits.length > 0 ? (\n                        <div className="absolute z-40 mt-1 max-h-56 w-full overflow-y-auto rounded-xl border border-slate-600 bg-[#0b1728] p-1 shadow-2xl">\n                          {custHits.map((customer) => (\n                            <button\n                              key={customer.id}\n                              type="button"\n                              onClick={() => {\n                                setForm((f) => ({\n                                  ...f,\n                                  customerId: customer.id,\n                                  customerCode: customer.customer_code || '',\n                                  customerName: customer.name || '',\n                                  customerPhone: customer.phone || '',\n                                  customerType: customer.segment || '',\n                                }));\n                                setCustSearch(customer.name || customer.customer_code || customer.phone || '');\n                                setCustHits([]);\n                                setCustSearched(false);\n                              }}\n                              className="block w-full rounded-lg px-2 py-2 text-right hover:bg-slate-800"\n                            >\n                              <div className="truncate text-xs font-black text-white">{customer.name || 'عميل بدون اسم'}</div>\n                              <div className="mt-0.5 truncate text-[10px] font-bold text-slate-400">\n                                {customer.customer_code || 'بدون كود'} · {customer.phone || 'بدون هاتف'}\n                              </div>\n                            </button>\n                          ))}\n                        </div>\n                      ) : null}\n                      {custSearched && custHits.length === 0 ? (\n                        <div className="mt-1 rounded-lg border border-amber-400/20 bg-amber-500/5 px-2 py-1 text-[10px] font-bold text-amber-200">\n                          لا يوجد عميل مطابق — اختاري «عميل جديد / يدوي» لو العميل جديد.\n                        </div>\n                      ) : null}\n                    </div>\n                  ) : (\n                    <div className="space-y-1">\n                      <input\n                        className="input-dark w-full"\n                        value={form.customerName}\n                        onChange={(e) => setForm((f) => ({ ...f, customerId: '', customerName: e.target.value }))}\n                        placeholder="اسم العميل الجديد"\n                        autoComplete="off"\n                      />\n                      <div className="grid grid-cols-2 gap-1">\n                        <input\n                          className="input-dark w-full"\n                          value={form.customerCode}\n                          onChange={(e) => setForm((f) => ({ ...f, customerId: '', customerCode: e.target.value }))}\n                          placeholder="الكود إن وجد"\n                        />\n                        <input\n                          className="input-dark w-full"\n                          value={form.customerPhone}\n                          onChange={(e) => setForm((f) => ({ ...f, customerId: '', customerPhone: e.target.value }))}\n                          placeholder="الموبايل إن وجد"\n                        />\n                      </div>\n                    </div>\n                  )}\n                </div>`
);

patch(
  'smart helper explains customer lookup',
  `                    للمحادثة السليمة: اختاري الدكتور، واكتبي اسم العميل لو متاح، ثم خدي قرار واحد. 1 = سليمة ≤5د بدون بيع وحفظ التالي. لو بيع: اختاري بيع، اكتبي الفاتورة، واعتمدي. أي ملاحظة فقط هي اللي تفتح التفاصيل. 2 = التفصيلي، 3 = البيانات الإضافية.`,
  `                    للمحادثة السليمة: اختاري الدكتور وابحثي عن العميل بالاسم أو الكود أو الموبايل. لو جديد استخدمي الإدخال اليدوي. بعدها خدي قرار واحد: 1 = سليمة ≤5د بدون بيع وحفظ التالي. لو بيع: اختاري بيع، اكتبي الفاتورة، واعتمدي. أي ملاحظة فقط هي اللي تفتح التفاصيل.`
);

fs.writeFileSync(file, src);
console.log('[smart-review-v9] customer lookup and manual fallback patched successfully');
