name: Apply review reassignment points patch

on:
  push:
    branches:
      - fix/reviews-full-details-editor
    paths:
      - '.github/workflows/apply-reviews-reassign-points.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/reviews-full-details-editor
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply patch
        run: python scripts/patch_reviews_reassign_points.py
      - name: Remove one-off helpers
        run: |
          rm scripts/patch_reviews_reassign_points.py
          rm .github/workflows/apply-reviews-reassign-points.yml
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: reconcile review points when manager changes doctor"
          git push origin HEAD:fix/reviews-full-details-editor
