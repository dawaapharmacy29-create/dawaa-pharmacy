name: Apply HR metric icon type v1

on:
  push:
    branches:
      - feat/hr-compliance-center-v1
    paths:
      - scripts/patch-hr-metric-icon-type-v1.cjs
      - .github/workflows/apply-hr-metric-icon-type-v1.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/hr-compliance-center-v1
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply TypeScript repair
        run: node scripts/patch-hr-metric-icon-type-v1.cjs
      - name: Remove one-time helpers
        run: rm -f scripts/patch-hr-metric-icon-type-v1.cjs .github/workflows/apply-hr-metric-icon-type-v1.yml
      - name: Commit repair
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/HRComplianceCenter.tsx scripts/patch-hr-metric-icon-type-v1.cjs .github/workflows/apply-hr-metric-icon-type-v1.yml
          git commit -m "fix(hr): type compliance metric icons explicitly" || exit 0
          git push origin HEAD:feat/hr-compliance-center-v1
